# TypeScript Assignment - B7A1(PH Level-2)

Solved 7 problems using various types in a single `solution.ts` file. With the additional two blog posts in `blog-#.md` files.

## Project Structure:

```
B7A1/
├── solutions.ts     # All 7 coding problems
├── blog-1.md        # Blog: unknown vs any & type narrowing
├── blog-2.md        # Blog: Pick & Omit utility types
└── README.md        # Project overview
```

## Functions, class and Methods written:

`filterEvenNumbers` | Array filtering |
`reverseString` | String manipulation |
`checkType` | Union types & type guards |
`getProperty` | Generics |
`toggleReadStatus` | Interfaces |
`Person` / `Student` | Class inheritance |
`getIntersection` | Array intersection |

## Blogs

- **blog-1.md** — Why is any labeled a "type safety hole," and why is unknown the safer choice for handling unpredictable data? Explain the concept of type narrowing.
- **blog-2.md** — How do Pick and Omit utility types prevent code duplication while creating specialized "slices" of a master interface? Discuss how this keeps your code DRY (Don't Repeat Yourself).
